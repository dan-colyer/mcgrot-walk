{
  "name": "mcgrot-walk",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "esbuild src/main.js --bundle --outfile=src/dev-bundle.js && node scripts/stamp-bundle.mjs",
    "dev": "npm run bundle && cd src && python3 ../scripts/serve.py ${PORT:-5174}",
    "bundle:mcgrots": "esbuild src/mcgrots/main.js --bundle --outfile=src/mcgrots-bundle.js && node scripts/stamp-mcgrots.mjs",
    "dev:mcgrots": "npm run bundle:mcgrots && cd src && python3 ../scripts/serve.py ${PORT:-5175}",
    "compare:mcgrots": "esbuild src/mcgrots/tools/compare-page.js --bundle --outfile=src/mcgrots-compare-bundle.js && cd src && python3 ../scripts/serve.py ${PORT:-5176}",
    "smoke:mcgrots": "node scripts/smoke-mcgrots.mjs",
    "styleshots:mcgrots": "node scripts/mcgrots-styleshots.mjs",
    "sun:mcgrots": "node scripts/mcgrots-sun.mjs",
    "smoke": "node scripts/smoke.mjs",
    "smoke:par": "node scripts/smoke.mjs --shards",
    "probe": "node scripts/probe.mjs",
    "style": "node scripts/style-sheet.mjs",
    "goldens:audit": "node scripts/goldens-audit.mjs",
    "goldens:noise": "node scripts/goldens-noise.mjs",
    "deploy": "node scripts/deploy.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "esbuild": "^0.28.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "pixelmatch": "^7.2.0",
    "playwright": "^1.61.1",
    "pngjs": "^7.0.0"
  }
}
